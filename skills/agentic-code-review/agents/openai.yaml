interface:
  display_name: "Agentic Code Review"
  short_description: "Review diffs and PRs for semantic regressions"
  default_prompt: "Use $agentic-code-review to review this diff or PR with semi-formal reasoning, surface semantic risks, and use swarm mode only when the change justifies it."
